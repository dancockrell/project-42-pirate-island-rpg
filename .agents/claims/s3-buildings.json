{
  "schema_version": 1,
  "task_id": "s3-buildings",
  "agent": "claude-code (lane S3)",
  "summary": "Buildings: standardized envelopes, sockets, tiers, construction, damage, capture and ruin, with every Open number a named constant marked needs decision.",
  "status": "active",
  "claimed_at": "2026-09-06T00:00:00Z",
  "updated_at": "2026-09-06T00:00:00Z",
  "paths": [
    "godot-rust/src/strategy/building.rs",
    "godot-rust/src/expedition.rs",
    "godot-rust/tests/save_migration.rs"
  ],
  "symbols": [
    "BuildingDefinition",
    "BuildingInstance",
    "BuildingState",
    "BuildingSocket",
    "ProductionRule",
    "ProductionOutput",
    "TierState",
    "CaptureRules",
    "RuinState",
    "BuildingError",
    "ExpeditionState.buildings",
    "ExpeditionState.place_building",
    "ExpeditionState.advance_construction",
    "ExpeditionState.upgrade_building",
    "ExpeditionState.damage_building",
    "ExpeditionState.capture_building",
    "ExpeditionState.ruin_building",
    "ExpeditionState.actor_sockets_at"
  ],
  "coordination": {
    "depends_on": ["s1-faction-definition", "s2-ownership-and-influence"],
    "conflicts_with": [],
    "notes": "expedition.rs is touched in exactly two hunks (a `buildings` field appended at the end of the struct and its initialiser at the end of the literal in new()) plus the use; every ExpeditionState method this lane needs lives in an impl block inside strategy/building.rs, because other lanes are appending to the same tail. force.rs is not edited: S7's ForceArrived aims at a socket through actor_sockets_at(cell_id), exposed here for B11. content/buildings/ is C10's and is not authored here."
  },
  "completion": {
    "commit": null,
    "checks": []
  }
}
