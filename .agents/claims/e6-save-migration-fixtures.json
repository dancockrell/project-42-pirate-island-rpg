{
  "schema_version": 1,
  "task_id": "e6-save-migration-fixtures",
  "agent": "claude-opus-5 (Claude Code, lane E6)",
  "summary": "E6: committed per-save_version JSON fixtures plus an integration test that proves every #[serde(default)] field still loads an older save",
  "status": "completed",
  "claimed_at": "2026-09-05T00:00:00Z",
  "updated_at": "2026-09-05T00:00:00Z",
  "paths": [
    "godot-rust/tests/saves/",
    "godot-rust/tests/save_migration.rs",
    ".agents/claims/e6-save-migration-fixtures.json"
  ],
  "symbols": [],
  "coordination": {
    "depends_on": [],
    "conflicts_with": [],
    "notes": "Read-only against godot-rust/src/**; A3 is appending anchor_uses to ExpeditionState on its own branch. The minimal v1 fixture must keep loading after that lands in either merge order. This claim adds no src edits: git diff godot-rust/src/ is empty at check-out. Forward-checked: temporarily appending anchor_uses: BTreeMap<String, u32> with #[serde(default)] kept all four save_migration tests green, then reverted; the minimal v1 fixture survives A3 in either merge order."
  },
  "completion": {
    "commit": "85413f0",
    "checks": [
      "cargo test --manifest-path godot-rust/Cargo.toml -- 111 unit + 4 save_migration + 1 vertical_slice, 0 failed: passed",
      "cargo fmt --manifest-path godot-rust/Cargo.toml -- --check -- no output: passed",
      "git diff --stat godot-rust/src/ -- empty, no src file touched by this branch: passed",
      "bite proof: #[serde(default)] removed from ExpeditionState.resolved_encounter_ids -- all 4 save_migration tests FAILED with MalformedJson(\"missing field `resolved_encounter_ids` at line 63 column 1\") against v1_minimal.json; attribute restored and expedition.rs byte-identical to origin: passed",
      "forward proof: anchor_uses: BTreeMap<String, u32> with #[serde(default)] appended (A3's incoming field) -- all 4 save_migration tests passed; reverted: passed",
      "fixtures generated from real state (ExpeditionState::new, resolve_midnight_in, inspect, travel x3, begin_encounter, resolve_encounter, to_json), not hand-written; the throwaway generator was deleted rather than committed: passed",
      "cargo check --manifest-path godot-rust/Cargo.toml --features godot-ext: not_run -- outside this task's scope, no src change to affect it",
      "node tools/src/validate.mjs / build-content-bundle.mjs: not_run -- this task touches no content/**"
    ]
  }
}
