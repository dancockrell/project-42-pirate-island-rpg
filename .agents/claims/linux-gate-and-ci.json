{
  "schema_version": 1,
  "task_id": "linux-gate-and-ci",
  "agent": "claude-code",
  "summary": "Portable Godot gate and native build for Linux/macOS, the first CI workflow, and the island suite completing again",
  "status": "completed",
  "claimed_at": "2026-09-09T00:00:00Z",
  "updated_at": "2026-09-09T11:45:00Z",
  "paths": [
    "tools/verify-godot.sh",
    "tools/build-native-bridge.sh",
    "game/bin/project42_sim.gdextension",
    ".github/workflows/verify.yml",
    ".gitignore",
    "godot-rust/src/godot_bridge.rs",
    "game/tests/island_scene_test.gd",
    "docs/RUNBOOK.md",
    "README.md"
  ],
  "symbols": [
    "Project42SimulationBridge.load_island"
  ],
  "coordination": {
    "depends_on": [],
    "conflicts_with": [],
    "notes": "Twins of the PowerShell gate and build script, same suites, same completion lines, same failure rules; the .gdextension gains linux and macos rows and game/bin/linux|macos are ignored rather than committed (the Windows DLLs stay tracked as they are). load_island now warns with the loader's stable reason token when it refuses a save; it refused silently before, which is how the island suite's synthetic removal stayed unexplained. The suite's removal step erased only the fort's obstacle while clearing every colonial holding; the loader rightly refuses an obstacle whose building is gone (invalid_saved_building_obstacles), and colonial expansion had raised a second holding by that point. The test now removes every colonial obstacle it clears and asserts the count it removed. No loader rule was loosened."
  },
  "completion": {
    "commit": "2e6e545",
    "checks": [
      "cargo fmt --manifest-path godot-rust/Cargo.toml -- --check: passed",
      "cargo test --manifest-path godot-rust/Cargo.toml (91 at the time of this claim): passed",
      "cargo check --manifest-path godot-rust/Cargo.toml --features godot-ext: passed",
      "node tools/src/validate.mjs (144 stable IDs) and build-content-bundle.mjs then git diff --quiet game/generated/: passed",
      "python3 tools/test_sprite_source.py and node tools/src/scan-staged-secrets.mjs --self-test: passed",
      "bash tools/build-native-bridge.sh debug produced game/bin/linux/project42_sim.linux.template_debug.x86_64.so at the path the .gdextension declares: passed",
      "bash tools/verify-godot.sh 60 with a local Godot 4.7.2: both suites, 18 PASS lines, 'PASS: island scene suite complete', no ERROR line: passed",
      "root cause of the recorded island_scene_test.gd:283 failure: the synthetic removal erased only the fort's obstacle while clearing every colonial holding, and colonial expansion had raised a second one, so the loader refused invalid_saved_building_obstacles. The test now removes every obstacle it clears and asserts the count removed; no loader rule was loosened: passed",
      "load_island warns with the loader's reason token on refusal; it returned a silent false before, which is why that failure went unexplained: passed",
      "CI run 34342614767 on ae740e8 FAILED the Godot job: a fresh clone has no .godot cache, so 'Could not resolve script res://scripts/simulation/native_simulation_port.gd'. Reproduced locally by deleting game/.godot: failed",
      "fix: one bounded --editor --quit import pass before the suites in both twins, which docs/RUNBOOK.md already claimed the verifier did. Re-proved from a deleted game/.godot; nine .uid files the pass generates are now tracked as their 22 siblings already were: passed",
      "CI run 34343228564 on 2e6e545: both jobs green (Rust and content; Godot headless suites with the real bridge): passed"
    ]
  }
}
